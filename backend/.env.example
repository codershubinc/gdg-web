# MongoDB
# Local:        mongodb://localhost:27017/gdg_csmu
# MongoDB Atlas: mongodb+srv://<user>:<pass>@cluster.mongodb.net/gdg_csmu?retryWrites=true&w=majority
MONGODB_URI=mongodb://localhost:27017/gdg_csmu

# JWT — use a long random string in production (32+ chars)
JWT_SECRET=change_me_to_a_long_random_secret_string

# Server
PORT=3001
NODE_ENV=development

# CORS — set to your frontend origin in production (e.g. https://gdg-csmu.vercel.app)
# Leave blank to allow all origins during development
CORS_ORIGIN=
