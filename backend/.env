# MongoDB
MONGODB_URI=mongodb://localhost:27017/gdg_csmu

# JWT
JWT_SECRET=gdg_campus_csmu_dev_secret_change_in_production

# Server
PORT=3001
NODE_ENV=development

# CORS (leave blank in dev = allow all)
CORS_ORIGIN=
